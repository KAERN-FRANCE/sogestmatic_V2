# Force Railway to use npm instead of pnpm
providers = []

[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
dependsOn = ["setup"]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
dependsOn = ["install"]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
